var StateMachine = require('../lib/state_machine')
  , sinon = require('sinon');

describe('StateMachine', function () {

  it('is a function', function () {
    expect(StateMachine).to.be.a('function');
  });
});
